# Champion Data REST API (Swagger: https://api.lacrosse.championdata.io/swagger/index.html)
# HTTP Basic — same as requests.get(..., auth=HTTPBasicAuth(username, password))
CHAMPION_DATA_USERNAME=
CHAMPION_DATA_PASSWORD=

# PLL Stats GraphQL API (https://api.stats.premierlacrosseleague.com/graphql)
PLL_STATS_API_KEY=

# Single shared password gating access to the app
TRACKER_PASSWORD=
